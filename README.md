# 🍳 RecipeManager — Intelligent Nutrition Analysis System

![.NET](https://img.shields.io/badge/.NET-8.0-purple.svg)
![React](https://img.shields.io/badge/React-19-blue.svg)
![EF Core](https://img.shields.io/badge/EF%20Core-9.0-green.svg)
![SQL Server](https://img.shields.io/badge/SQL%20Server-2019+-CC2927.svg)

Full-stack платформа для управления кулинарным контентом с интеллектуальной системой анализа нутриентов и нечетким поиском ингредиентов.

## 🚀 Ключевые инженерные решения

### 🧠 Интеллектуальный поиск и сопоставление (Алгоритм Левенштейна)
Реализован модуль сопоставления ингредиентов с использованием алгоритма **Левенштейна** ($O(n \times m)$). Это позволяет выполнять нечеткий поиск по локальному словарю нутриентов, значительно снижая количество дорогостоящих вызовов к внешнему Nutrition API и повышая скорость отклика системы.

### 🛡️ API Resilience & 4-уровневая стратегия Fallback
Для обеспечения надежности данных о питательной ценности внедрена многоуровневая стратегия обработки запросов к внешним сервисам (API Ninjas):
1. **Direct Match** — поиск по оригинальному названию.
2. **Transliteration** — автоматическая конвертация кириллицы в латиницу.
3. **Regex Normalization** — очистка строк через **Compiled Regex**.
4. **Partial Search** — поиск по ключевым словам.

### ⚡ Оптимизация производительности и кэширование
- **Memory Caching:** Внедрено кэширование часто запрашиваемых справочников (категории, теги) с нормализацией ключей.
- **EF Core Optimization:** Использование **AsSplitQuery** для предотвращения **Cartesian Explosion** в сложных графах данных и `AsNoTracking` для ускорения Read-only операций.
- **AutoMapper AfterMap:** Инкапсуляция логики тяжелых расчетов КБЖУ «на лету» внутри профилей маппинга, что позволило сохранить доменные модели «чистыми».

### 💎 Надежность данных и Concurrency
Реализована защита от конфликтов параллельного редактирования через **Optimistic Concurrency (RowVersion)**. Система отслеживает версии сущностей, предотвращая потерю данных при одновременной правке рецептов несколькими пользователями. Обеспечена полная поддержка **CancellationTokens** во всех асинхронных цепочках для эффективного управления ресурсами сервера.

### ⚛️ Современный Frontend (React 19)
- **Real-time расчеты:** Использование **useMemo** для мгновенного пересчета нутриентов при изменении веса ингредиентов в UI.
- **State Management:** Управление состоянием аутентификации и доступа через **Context API**.
- **Fault Tolerance:** Механизм автосохранения черновиков в **localStorage** для предотвращения потери прогресса при сбоях сети или перезагрузке страницы.

## 🛠️ Технологический стек

- **Backend:** .NET 8, ASP.NET Core Web API, EF Core 9.
- **Architecture:** Repository + Unit of Work, JWT (Cookie-based), RBAC.
- **Frontend:** React 19, Vite, Axios, Bootstrap 5.
- **Storage:** MS SQL Server.
- **Tools:** Swagger, AutoMapper, Compiled Regex, Fluent API.

## 🏗️ Структура проекта

- `backend/` — RESTful API с четким разделением на слои (Entities, DTOs, Repositories, Services).
- `frontend/` — SPA на React 19 с защищенными маршрутами (ProtectedRoutes).

## 🚀 Быстрый старт

1. Настройте `SecretKey` и `ConnectionStrings` в `appsettings.json`.
2. Обновите базу данных:
   ```bash
   dotnet ef database update
Запустите Backend:
   ```bash
   dotnet run
```
Запустите Frontend:
   ```bash
   npm install && npm run dev
